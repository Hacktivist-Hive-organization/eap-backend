# .env.test (used for CI and local tests)
DATABASE_TYPE=sqlite
DATABASE_SCHEMA=:memory:   # In-memory SQLite for tests
DATABASE_ECHO=False

# Dummy values (not used for SQLite)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=test_user
DATABASE_PASSWORD=test_pass

APP_NAME=EAP
APP_VERSION=1.0
API_V1_PREFIX=/api/v1